 
-------create static binary----------------
gcc main.c SMBus/SMBus.c -static -L. libF75111.a  -pthread -lpthread -o CIO_LED_FLASH
-------------------------------------------

-------create shared binary----------------
gcc main.c SMBus/SMBus.c -L. libF75111.so -pthread -o CIO_LED_FLASH
-------------------------------------------

-------run binary with shared library------
sudo LD_LIBRARY_PATH=. ./CIO_LED_FLASH
-------------------------------------------

